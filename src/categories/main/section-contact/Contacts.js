import React from 'react';
import Coordinates from '../../../components/main/section-contact/Coordinates';

function Contacts() {
  return (
    <section className="contacts" id="contacts">
      <h2 className="contacts-title">Contacts</h2>
      <Coordinates />
    </section>
  );
}

export default Contacts;
